<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>WhisperCircle</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg-primary: #0f0a1f;
      --bg-secondary: #1a1435;
      --accent-primary: #8b5cf6;
      --accent-secondary: #7c3aed;
      --text-primary: #f5f3ff;
      --text-secondary: #ddd6fe;
      --gradient: linear-gradient(135deg, #7c3aed 0%, #a78bfa 100%);
    }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      margin: 0;
      padding: 0;
      background-color: var(--bg-primary);
      color: var(--text-primary);
      line-height: 1.6;
    }
    
    header {
      background-color: rgba(15, 10, 31, 0.8);
      backdrop-filter: blur(10px);
      box-shadow: 0 4px 30px rgba(0, 0, 0, 0.3);
      padding: 20px 5%;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: sticky;
      top: 0;
      z-index: 100;
      border-bottom: 1px solid rgba(139, 92, 246, 0.1);
    }
    
    h1 {
      margin: 0;
      color: var(--text-primary);
      font-size: 1.8em;
      font-weight: 700;
      background: var(--gradient);
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    
    nav {
      display: flex;
      gap: 15px;
    }
    
    nav a {
      padding: 12px 24px;
      text-decoration: none;
      font-weight: 500;
      border-radius: 8px;
      transition: all 0.3s ease;
      font-size: 0.95em;
    }
    
    nav a.login {
      color: var(--accent-primary);
      border: 1px solid rgba(139, 92, 246, 0.3);
    }
    
    nav a.signup {
      background: var(--gradient);
      color: white;
      box-shadow: 0 4px 15px rgba(123, 92, 246, 0.3);
    }
    
    nav a.member {
      background-color: var(--bg-secondary);
      color: var(--accent-primary);
      border: 1px solid rgba(139, 92, 246, 0.2);
    }
    
    nav a:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(123, 92, 246, 0.4);
    }
    
    main {
      text-align: center;
      margin: 80px auto;
      max-width: 900px;
      padding: 0 5%;
    }
    
    h2 {
      font-size: 3em;
      margin-bottom: 20px;
      font-weight: 700;
      background: var(--gradient);
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
      line-height: 1.2;
    }
    
    p {
      font-size: 1.25em;
      color: var(--text-secondary);
      margin-bottom: 50px;
      max-width: 700px;
      margin-left: auto;
      margin-right: auto;
      font-weight: 300;
    }
    
    .hero-image {
      width: 70%;
      max-width: 600px;
      border-radius: 16px;
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
      border: 1px solid rgba(139, 92, 246, 0.2);
      margin-top: 40px;
      transition: transform 0.5s ease;
    }
    
    .hero-image:hover {
      transform: scale(1.02);
    }
    
    .cta-container {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 40px;
    }
    
    .cta-primary {
      padding: 16px 32px;
      background: var(--gradient);
      color: white;
      border: none;
      border-radius: 12px;
      font-weight: 600;
      font-size: 1em;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(123, 92, 246, 0.3);
    }
    
    .cta-secondary {
      padding: 16px 32px;
      background: rgba(139, 92, 246, 0.1);
      color: var(--accent-primary);
      border: 1px solid rgba(139, 92, 246, 0.3);
      border-radius: 12px;
      font-weight: 600;
      font-size: 1em;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    
    .cta-primary:hover, .cta-secondary:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 25px rgba(123, 92, 246, 0.4);
    }
    
    footer {
      margin-top: 100px;
      padding: 30px 5%;
      text-align: center;
      font-size: 0.9em;
      color: var(--text-secondary);
      background-color: var(--bg-secondary);
      border-top: 1px solid rgba(139, 92, 246, 0.1);
    }
    
    @media (max-width: 768px) {
      header {
        flex-direction: column;
        gap: 15px;
      }
      
      nav {
        width: 100%;
        justify-content: center;
      }
      
      h2 {
        font-size: 2.2em;
      }
      
      .hero-image {
        width: 90%;
      }
      
      .cta-container {
        flex-direction: column;
        align-items: center;
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>WhisperCircle</h1>
    <nav>
      <a href="/login" class="login">Login</a>
      <a href="/sign-up" class="signup">Sign Up</a>
      <a href="/become-member" class="member">Become a Member</a>
    </nav>
  </header>

  <main>
    <h2>Your Private Space for Meaningful conversation</h2>
    <p>
      A private, secure space where members connect, share meaningful messages, and build a trusted community. Join the circle, be heard, and keep your secrets safe.
    </p>
    
    <div class="cta-container">
        <a href="/sign-up">
            <button class="cta-primary">Join the Community</button>
        </a>
    </div>
    
    <img src="community.svg" alt="WhisperCircle community illustration" class="hero-image" />
  </main>
  <a href="/become-member">member</a>
  <footer>
    &copy; 2025 WhisperCircle. All rights reserved. | <a href="/privacy" style="color: var(--accent-primary); text-decoration: none;">Privacy Policy</a>
  </footer>
</body>
</html>