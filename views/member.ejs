<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Become the member WhisperCircle</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --bg-primary: #0f0a1f;
        --bg-secondary: #1a1435;
        --accent-primary: #8b5cf6;
        --accent-secondary: #7c3aed;
        --text-primary: #f5f3ff;
        --text-secondary: #ddd6fe;
        --gradient: linear-gradient(135deg, #7c3aed 0%, #a78bfa 100%);
      }

      body {
        font-family: "Inter", -apple-system, BlinkMacSystemFont, sans-serif;
        margin: 0;
        padding: 0;
        background-color: var(--bg-primary);
        color: var(--text-primary);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        overflow-y: hidden;
      }

      header {
        background-color: rgba(15, 10, 31, 0.8);
        backdrop-filter: blur(10px);
        padding: 20px 5%;
        border-bottom: 1px solid rgba(139, 92, 246, 0.1);
      }

      .logo {
        font-size: 1.8em;
        font-weight: 700;
        background: var(--gradient);
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
        text-decoration: none;
      }

      .login-container {
        display: flex;
        flex: 1;
      }

      .login-form-container {
        flex: 1;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .login-form {
        background: var(--bg-secondary);
        border-radius: 16px;
        display: flex;
        padding: 40px 40px;
        flex-direction: column;
        justify-content: center;
        width: 100%;
        max-width: 450px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        border: 1px solid rgba(139, 92, 246, 0.2);
      }

      .image-container {
        flex: 1;
        background: linear-gradient(
          rgba(15, 10, 31, 0.7),
          rgba(15, 10, 31, 0.7)
        );
        background-size: cover;
        background-position: center;
        display: flex;
        justify-content: center;
        align-items: center;
        transform: scale(0.8);
      }

      h4 {
        margin-top: 0;
        margin-bottom: 30px;
        font-size: 1.5rem;
        background: var(--gradient);
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
      }

      .form-group {
        margin-bottom: 20px;
      }

      label {
        display: block;
        margin-bottom: 8px;
        font-weight: 500;
        color: var(--text-secondary);
      }

      input {
        width: 92%;
        padding: 12px 16px;
        border-radius: 8px;
        border: 1px solid rgba(139, 92, 246, 0.3);
        background: rgba(15, 10, 31, 0.5);
        color: var(--text-primary);
        font-family: inherit;
        font-size: 1em;
        transition: border-color 0.3s, box-shadow 0.3s;
      }

      input:focus {
        outline: none;
        border-color: var(--accent-primary);
        box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.2);
      }

      .forgot-password {
        text-align: right;
        margin-top: -15px;
        margin-bottom: 20px;
      }

      .forgot-password a {
        color: var(--accent-primary);
        text-decoration: none;
        font-size: 0.9em;
      }

      .forgot-password a:hover {
        text-decoration: underline;
      }

      button {
        width: 100%;
        padding: 14px;
        border: none;
        border-radius: 8px;
        font-family: inherit;
        font-size: 1em;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s;
      }

      .login-btn {
        background: var(--gradient);
        color: white;
        box-shadow: 0 4px 15px rgba(123, 92, 246, 0.3);
      }

      .login-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(123, 92, 246, 0.4);
      }

      .signup-link {
        text-align: center;
        margin-top: 20px;
        color: var(--text-secondary);
      }

      .signup-link a {
        color: var(--accent-primary);
        text-decoration: none;
        font-weight: 500;
      }

      .signup-link a:hover {
        text-decoration: underline;
      }

      footer {
        text-align: center;
        padding: 20px;
        color: var(--text-secondary);
        font-size: 0.9em;
        background-color: var(--bg-secondary);
        border-top: 1px solid rgba(139, 92, 246, 0.1);
        position: fixed;
        bottom: 0;
        width: 100vw;
      }

      @media (max-width: 900px) {
        .image-container {
          display: none;
        }
      }

      @media (max-width: 600px) {
        .login-form {
          padding: 30px 20px;
        }
      }
      #error {
        color: rgba(255, 0, 0, 0.71);
        text-align: center;
      }
    </style>
  </head>
  <body>
    <header>
      <a href="/" class="logo">WhisperCircle</a>
    </header>

    <div class="login-container">
      <div class="login-form-container">
        <div class="login-form">
          <h4>
            Become a member to enjoy exclusive access to our premium services!
          </h4>
          <form id="loginForm" action="/become-member" method="POST">
            <div class="form-group">
              <label for="secretCode">Secret code</label>
              <input type="text" id="secretCode" name="secretCode" required />
            </div>

            <% if(locals.errors){ %>
            <div id="error"><%= errors %></div>

            <%}%>

            <button type="submit" class="login-btn">become member</button>
          </form>
        </div>
      </div>

      <div class="image-container">
        <img src="member.svg" alt="img" />
      </div>
    </div>

    <footer>&copy; 2025 WhisperCircle. All rights reserved.</footer>
  </body>
</html>
