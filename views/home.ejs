<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>WhisperCircle Messages</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --bg-primary: #0f0a1f;
        --bg-secondary: #1a1435;
        --accent-primary: #8b5cf6;
        --accent-secondary: #7c3aed;
        --text-primary: #f5f3ff;
        --text-secondary: #ddd6fe;
        --gradient: linear-gradient(135deg, #7c3aed 0%, #a78bfa 100%);
        --success: #10b981;
      }

      body {
        font-family: "Inter", -apple-system, BlinkMacSystemFont, sans-serif;
        margin: 0;
        padding: 0;
        background-color: var(--bg-primary);
        color: var(--text-primary);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      header {
        background-color: rgba(15, 10, 31, 0.8);
        backdrop-filter: blur(10px);
        padding: 20px 5%;
        border-bottom: 1px solid rgba(139, 92, 246, 0.1);
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: fixed;
        top: 0;
        left: 0;
        width: 90%;
        z-index: 100;
      }

      .logo {
        font-size: 1.8em;
        font-weight: 700;
        background: var(--gradient);
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
        text-decoration: none;
      }

      .header-actions {
        display: flex;
        gap: 15px;
      }

      .header-btn {
        padding: 10px 20px;
        border-radius: 8px;
        font-weight: 500;
        text-decoration: none;
        transition: all 0.3s;
        font-size: 0.95em;
      }

      .logout-btn {
        background: rgba(239, 68, 68, 0.1);
        color: #ef4444;
        border: 1px solid rgba(239, 68, 68, 0.3);
      }

      .logout-btn:hover {
        background: rgba(239, 68, 68, 0.2);
      }

      .member-btn {
        background: var(--gradient);
        color: white;
        box-shadow: 0 4px 15px rgba(123, 92, 246, 0.3);
      }

      .member-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(123, 92, 246, 0.4);
      }



      main {
        flex: 1;
        padding: 40px 5%;
        max-width: 1200px;
        margin: 0 auto;
        width: 100%;
      }

      .page-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
      }

      h1 {
        margin: 0;
        font-size: 2.2em;
        background: var(--gradient);
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
      }
      .new-message-input-bar {
        background-color: var(--bg-secondary);
        padding: 20px 5%;
        border-top: 1px solid rgba(139, 92, 246, 0.1);
        display: flex;
        justify-content: center;
        position: fixed;
        bottom: 0;
        z-index: 100;
        width: 100%;
      }

      .new-message-input-bar form {
        display: flex;
        gap: 10px;
        width: 100%;
        max-width: 900px;
      }

      .new-message-input-bar input[type="text"] {
        flex: 1;
        padding: 12px 16px;
        border-radius: 8px;
        border: 1px solid rgba(139, 92, 246, 0.3);
        background-color: #221b3f;
        color: var(--text-primary);
        font-size: 1em;
      }

      .new-message-input-bar input[type="text"]::placeholder {
        color: var(--text-secondary);
      }

      .new-message-input-bar button {
        background: var(--gradient);
        color: white;
        border: none;
        padding: 12px 20px;
        border-radius: 8px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s;
      }

      .new-message-input-bar button:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(123, 92, 246, 0.4);
      }

      .messages-container {
        display: flex;
        flex-direction: column;
        gap: 25px;
        margin-top: 4rem;
        margin-bottom: 4rem;
      }

      .message-card {
        background: var(--bg-secondary);
        border-radius: 12px;
        padding: 25px;
        border: 1px solid rgba(139, 92, 246, 0.2);
        transition: transform 0.3s, box-shadow 0.3s;
      }

      .message-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
      }

      .message-title {
        font-size: 1.4em;
        margin-top: 0;
        margin-bottom: 15px;
        color: var(--text-primary);
      }

      .message-content {
        color: var(--text-secondary);
        margin-bottom: 20px;
        line-height: 1.6;
      }

      .message-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 0.9em;
        color: var(--accent-primary);
        border-top: 1px solid rgba(139, 92, 246, 0.1);
        padding-top: 15px;
      }

      .admin-controls {
        display: flex;
        justify-content: flex-end;
        margin-top: 15px;
      }

      .delete-btn {
        background: rgba(239, 68, 68, 0.1);
        color: #ef4444;
        border: none;
        padding: 6px 12px;
        border-radius: 6px;
        font-size: 0.85em;
        cursor: pointer;
        transition: background 0.3s;
      }

      .delete-btn:hover {
        background: rgba(239, 68, 68, 0.2);
      }

      .member-only {
        background: rgba(16, 185, 129, 0.1);
        color: var(--success);
        padding: 3px 8px;
        border-radius: 4px;
        font-size: 0.8em;
        font-weight: 500;
      }

      footer {
        text-align: center;
        padding: 20px;
        color: var(--text-secondary);
        font-size: 0.9em;
        background-color: var(--bg-secondary);
        border-top: 1px solid rgba(139, 92, 246, 0.1);
      }

      @media (max-width: 768px) {
        .messages-container {
          grid-template-columns: 1fr;
        }

        .page-header {
          flex-direction: column;
          align-items: flex-start;
          gap: 20px;
        }
      }
    </style>
  </head>
  <body>
    <header>
      <a href="/" class="logo">WhisperCircle</a>
      <div class="header-actions">
        <a href="/become-member" class="header-btn member-btn">Become Member</a>
        <a href="/logout" class="header-btn logout-btn">Log Out</a>
      </div>
    </header>

    <main>
      

      <div class="messages-container">
        <!-- Sample Message 1 -->
        <div class="message-card">
          <h3 class="message-title">Welcome to WhisperCircle</h3>
          <p class="message-content">
            This is your first message in our exclusive community. Share your
            thoughts, connect with other members, and enjoy the private space
            we've created.
          </p>
          <div class="message-footer">
            <span class="member-only">Members Only</span>
            <span>2 days ago</span>
          </div>
          <!-- Admin controls (only visible to admins) -->
          <div class="admin-controls">
            <button class="delete-btn">Delete Message</button>
          </div>
        </div>

        <!-- Sample Message 2 -->
        <div class="message-card">
          <h3 class="message-title">Community Guidelines</h3>
          <p class="message-content">
            Please remember to be respectful to all members. We value thoughtful
            discussions and meaningful connections in this space.
          </p>
          <div class="message-footer">
            <span>Anonymous</span>
            <span>1 week ago</span>
          </div>
        </div>

        <!-- Sample Message 3 -->
        <div class="message-card">
          <h3 class="message-title">Upcoming Event</h3>
          <p class="message-content">
            Join us next Friday for our virtual meetup. Members will receive the
            invitation link via email. Looking forward to seeing everyone there!
          </p>
          <div class="message-footer">
            <span class="member-only">Members Only</span>
            <span>3 days ago</span>
          </div>
        </div>
      </div>
    </main>
    <div class="new-message-input-bar">
      <form action="/post-message" method="POST">
        <input
          type="text"
          name="message"
          placeholder="Type your message..."
          required
        />
        <button type="submit">Send</button>
      </form>
    </div>
  </body>
</html>
